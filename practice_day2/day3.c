#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - Entry point to my morning check out project
 * project purposed for while nested loops
 * return: always 0
 */
int main(void)
{
	int n = 0;
	while (i < 5) {
		printf("%d\n", i);
		i++
	}
	return 0;
}
